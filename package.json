{
  "scripts": {
    "start": "parcel serve --host 0.0.0.0 ./src/**/*.html",
    "build": "parcel build ./src/index.css --public-url ./ --dist-dir css",
    "clean": "rm -rf .parcel-cache/ build/ dist/ css/"
  },
  "name": "blog-theme",
  "version": "0.1.0",
  "description": "My blog theme",
  "author": "Santiago Fraire <santiwilly@gmail.com>",
  "license": "AGPL-3.0",
  "private": false,
  "dependencies": {
    "tailwindcss": "^1.4.6"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  }
}
